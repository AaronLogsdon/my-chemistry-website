import React, { Component } from 'react';//making the component feature available

const Home = () => (//displays the word "Home" near top of page
  <div>
    <h1>Home</h1>
  </div>
);

export default (Home);//creates a means of making the Home file available to other files